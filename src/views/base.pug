doctype html
html(lang="ko")
  head
    title #{title} | Wetube
    link(rel="stylesheet" href="https://unpkg.com/mvp.css")
  body
    header
      nav 
        ul 
          li
            a(href="/") Home
          li
            a(href="/join") Join
          li
            if user.loggedIn
              a(href="/logout") Logout
            else
              a(href="/login") Login
      h1=title
    main
      h1=h1
    include partials/footer 