extends base

block content
  h4 Change Title of  video: #{video.title}
  if errorMessage
    span=errorMessage
  form(method="POST", )
    input(placeholder="Video Title", type="text", name="title", value=video.title required)
    input(type="submit", value="Save")